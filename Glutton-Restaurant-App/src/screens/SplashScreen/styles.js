import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    Container: {
        flex: 1,
    },
    BackgroundImage: {
        width: '100%',
        height: '100%',
        position: 'absolute',
        zIndex: -10,
    }
})