import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    Container: {
        flex: 1,
    },
    ContentContainer: {
        paddingHorizontal: 15,
        paddingTop: 55,
        paddingBottom: 30,
    },
})