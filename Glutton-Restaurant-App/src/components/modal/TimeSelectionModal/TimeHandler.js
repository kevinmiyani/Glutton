import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TimeHandler = ({

}) => {
    return (
        <View>
            <Text>TimeHandler</Text>
        </View>
    )
}

export default TimeHandler

const styles = StyleSheet.create({})